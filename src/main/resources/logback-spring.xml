<configuration scan="true">

    <!-- Boot helpers (color converter, defaults) -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Pull Spring props -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name"/>
    <springProperty scope="context" name="ENV" source="spring.profiles.active"/>
    <springProperty scope="context" name="ROOT_LEVEL" source="logging.level.root"/>

    <!-- ===== PROD: JSON to STDOUT (CloudWatch/Fluent Bit friendly) ===== -->
    <springProfile name="prod">
        <appender name="STDOUT_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"service":"${APP_NAME:-app}","env":"${ENV:-prod}"}</customFields>
                <includeStructuredArguments>true</includeStructuredArguments>
            </encoder>
        </appender>

        <appender name="ASYNC_STDOUT_JSON" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>8192</queueSize>
            <neverBlock>true</neverBlock>
            <appender-ref ref="STDOUT_JSON"/>
        </appender>

        <root level="${ROOT_LEVEL:WARN}">
            <appender-ref ref="ASYNC_STDOUT_JSON"/>
        </root>

        <!-- Tame noisy libs -->
        <logger name="org.springframework" level="WARN"/>
        <logger name="com.popovrnd" level="INFO"/>
    </springProfile>

    <!-- ===== DEV: pretty text to console with trace/span ===== -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>
                    %d{HH:mm:ss.SSS} %clr(%-5level) [%thread] %cyan(%logger{36}) - %msg
                    traceId=%X{traceId} spanId=%X{spanId}%n
                </pattern>
            </encoder>
        </appender>

        <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>4096</queueSize>
            <appender-ref ref="CONSOLE"/>
        </appender>

        <root level="${ROOT_LEVEL:WARN}">
            <appender-ref ref="ASYNC_CONSOLE"/>
        </root>

        <logger name="org.springframework" level="WARN"/>
        <logger name="com.popovrnd" level="DEBUG"/>
    </springProfile>

</configuration>
